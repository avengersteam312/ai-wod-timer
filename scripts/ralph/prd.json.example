{
  "project": "AI Workout Timer",
  "branchName": "ralph/persistent-timers",
  "description": "Persistent Timers Feature - Allow users to save and name timer configurations before starting workouts",
  "userStories": [
    {
      "id": "AVE-15",
      "title": "Add save timer configuration dialog",
      "description": "As a user, I want to save my timer configuration with a custom name before starting a workout, so I can reuse it later.",
      "acceptanceCriteria": [
        "Add 'Save Timer' button to frontend/src/components/manual-timer/ManualTimerSetup.vue",
        "Button opens a dialog/modal asking for timer name",
        "Dialog validates name is not empty and max 50 characters",
        "Dialog has Save and Cancel buttons",
        "Typecheck passes (vue-tsc --noEmit)",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 1,
      "passes": false,
      "notes": ""
    },
    {
      "id": "AVE-15-1",
      "title": "Create API endpoint to save timer configurations",
      "description": "As a developer, I need an API endpoint to persist timer configurations with names.",
      "acceptanceCriteria": [
        "Add POST /api/v1/timer-configs endpoint in backend/app/api/v1/endpoints/",
        "Endpoint accepts: name, timer_type, config (JSON), user_id",
        "Endpoint requires Firebase authentication",
        "Returns saved config with ID and created_at timestamp",
        "Typecheck passes (pytest runs without errors)"
      ],
      "priority": 2,
      "passes": false,
      "notes": ""
    },
    {
      "id": "AVE-15-2",
      "title": "Add database model for timer configurations",
      "description": "As a developer, I need to store timer configurations in the database.",
      "acceptanceCriteria": [
        "Create TimerConfig model in backend/app/models/timer_config.py",
        "Add SQLAlchemy fields: id, user_id, name, timer_type, config (JSONB), created_at, updated_at",
        "Generate and run Alembic migration successfully",
        "Typecheck passes (pytest runs without errors)"
      ],
      "priority": 3,
      "passes": false,
      "notes": ""
    },
    {
      "id": "AVE-15-3",
      "title": "Add saved timers list to manual timer setup",
      "description": "As a user, I want to see my saved timer configurations and load them.",
      "acceptanceCriteria": [
        "Add GET /api/v1/timer-configs endpoint to fetch user's saved configs",
        "Display saved timers list above timer type selector in ManualTimerSetup.vue",
        "Show timer name, type, and date created",
        "Clicking a saved timer loads its configuration",
        "Add delete button for each saved timer",
        "Typecheck passes (vue-tsc --noEmit)",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 4,
      "passes": false,
      "notes": ""
    },
    {
      "id": "AVE-19",
      "title": "Make timer display responsive for large screens",
      "description": "As a user, I want the timer interface to adapt properly to large screens (tablets, desktops) for better visibility.",
      "acceptanceCriteria": [
        "Update frontend/src/components/timer/TimerDisplay.vue with responsive breakpoints",
        "Timer text scales appropriately on screens > 1024px",
        "Layout uses max-width container on large screens",
        "Movement blocks arrange in grid on large screens instead of stacked",
        "Test on viewport widths: 768px, 1024px, 1440px, 1920px",
        "Typecheck passes (vue-tsc --noEmit)",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 5,
      "passes": false,
      "notes": ""
    },
    {
      "id": "AVE-9",
      "title": "Create separate module for default workout configurations",
      "description": "As a developer, I want default workout configurations in a dedicated module for easier maintenance and extension.",
      "acceptanceCriteria": [
        "Create frontend/src/config/defaultWorkouts.ts module",
        "Move default workout configs from components to this module",
        "Export workout type definitions and default values",
        "Update components to import from new module",
        "Ensure no functionality is broken",
        "Typecheck passes (vue-tsc --noEmit)"
      ],
      "priority": 6,
      "passes": false,
      "notes": ""
    }
  ]
}
